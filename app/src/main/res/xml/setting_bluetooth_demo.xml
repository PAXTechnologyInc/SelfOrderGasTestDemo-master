<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android"
    android:key="@string/bluetooth_setting_prescreen">
    <Preference
        android:key="@string/bluetooth_setting_list"
        android:title="Bluetooth Setting"
        android:defaultValue="#00FFFF"
        android:entries="@array/foreground_color_names_array"
        android:entryValues="@array/foreground_color_values_array"
        android:summary="Select a foreground color"
        android:icon="@drawable/circle_red"
        android:dialogIcon="@drawable/error_circle"
        />
<!--
    <GridView
        android:id="@+id/grid"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:background="@color/white"
        android:columnWidth="50dp"

        android:gravity="center"
        android:horizontalSpacing="10dp"
        android:numColumns="3"
        android:theme="@android:style/Holo.ButtonBar"
        android:verticalSpacing="10dp" />
        -->
</PreferenceScreen>